{
  "linear-api": {
    "$comment": "All requests to the Linear API.",
    "properties": {
      "domain": {
        "const": "api.linear.app"
      }
    },
    "required": [
      "domain"
    ]
  }
}
