{
  "yelp-api": {
    "$comment": "Any request to the Yelp API.",
    "properties": {
      "domain": {
        "const": "api.yelp.com"
      }
    },
    "required": [
      "domain"
    ]
  },
  "yelp-read-all": {
    "$comment": "Read any Yelp data.",
    "properties": {
      "method": {
        "const": "GET"
      }
    },
    "required": [
      "method"
    ]
  },
  "yelp-write-all": {
    "$comment": "Create, update, or delete any Yelp data.",
    "properties": {
      "method": {
        "enum": [
          "POST",
          "PUT",
          "PATCH",
          "DELETE"
        ]
      }
    },
    "required": [
      "method"
    ]
  },
  "yelp-read-businesses": {
    "$comment": "Look up businesses and transactions.",
    "properties": {
      "method": {
        "const": "GET"
      },
      "path": {
        "type": "string",
        "pattern": "^/v3/(businesses|transactions)(/.*)?$"
      }
    },
    "required": [
      "method",
      "path"
    ]
  },
  "yelp-write-businesses": {
    "$comment": "Create, update, or delete businesses and transactions.",
    "properties": {
      "method": {
        "enum": [
          "POST",
          "PUT",
          "PATCH",
          "DELETE"
        ]
      },
      "path": {
        "type": "string",
        "pattern": "^/v3/(businesses|transactions)(/.*)?$"
      }
    },
    "required": [
      "method",
      "path"
    ]
  },
  "yelp-read-reviews": {
    "$comment": "Read reviews and review highlights for a business.",
    "properties": {
      "method": {
        "const": "GET"
      },
      "path": {
        "type": "string",
        "pattern": "^/v3/businesses/[^/]+/(reviews|review_highlights)(/.*)?$"
      }
    },
    "required": [
      "method",
      "path"
    ]
  },
  "yelp-read-events": {
    "$comment": "Browse and search Yelp events.",
    "properties": {
      "method": {
        "const": "GET"
      },
      "path": {
        "type": "string",
        "pattern": "^/v3/events(/.*)?$"
      }
    },
    "required": [
      "method",
      "path"
    ]
  },
  "yelp-read-categories": {
    "$comment": "List or look up business categories.",
    "properties": {
      "method": {
        "const": "GET"
      },
      "path": {
        "type": "string",
        "pattern": "^/v3/categories(/.*)?$"
      }
    },
    "required": [
      "method",
      "path"
    ]
  },
  "yelp-autocomplete": {
    "$comment": "Get autocomplete suggestions.",
    "properties": {
      "method": {
        "const": "GET"
      },
      "path": {
        "type": "string",
        "pattern": "^/v3/autocomplete$"
      }
    },
    "required": [
      "method",
      "path"
    ]
  },
  "yelp-ai-chat": {
    "$comment": "Send messages to the Yelp AI chat.",
    "properties": {
      "method": {
        "const": "POST"
      },
      "path": {
        "type": "string",
        "pattern": "^/ai/chat(/.*)?$"
      }
    },
    "required": [
      "method",
      "path"
    ]
  }
}
