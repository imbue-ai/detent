{
  "google-directions-api": {
    "$comment": "Matches all Google Routes API traffic.",
    "properties": {
      "domain": {
        "const": "routes.googleapis.com"
      }
    },
    "required": [
      "domain"
    ]
  },
  "google-directions-read-all": {
    "$comment": "Read any Google Directions data.",
    "properties": {
      "method": {
        "const": "GET"
      }
    },
    "required": [
      "method"
    ]
  },
  "google-directions-write-all": {
    "$comment": "Create, update, or delete any Google Directions data.",
    "properties": {
      "method": {
        "enum": [
          "POST",
          "PUT",
          "PATCH",
          "DELETE"
        ]
      }
    },
    "required": [
      "method"
    ]
  },
  "google-directions-compute-routes": {
    "$comment": "Compute routes between locations.",
    "properties": {
      "method": {
        "const": "POST"
      },
      "path": {
        "type": "string",
        "pattern": "^/directions/v[0-9]+:computeRoutes$"
      }
    },
    "required": [
      "method",
      "path"
    ]
  },
  "google-directions-compute-route-matrix": {
    "$comment": "Compute a distance and duration matrix between multiple origins and destinations.",
    "properties": {
      "method": {
        "const": "POST"
      },
      "path": {
        "type": "string",
        "pattern": "^/distanceMatrix/v[0-9]+:computeRouteMatrix$"
      }
    },
    "required": [
      "method",
      "path"
    ]
  }
}
