{
  "sentry-api": {
    "$comment": "Any request to the Sentry API.",
    "properties": {
      "domain": {
        "const": "sentry.io"
      }
    },
    "required": [
      "domain"
    ]
  },
  "sentry-read-all": {
    "$comment": "Read any Sentry data.",
    "properties": {
      "method": {
        "const": "GET"
      }
    },
    "required": [
      "method"
    ]
  },
  "sentry-write-all": {
    "$comment": "Create, update, or delete any Sentry data.",
    "properties": {
      "method": {
        "enum": [
          "POST",
          "PUT",
          "PATCH",
          "DELETE"
        ]
      }
    },
    "required": [
      "method"
    ]
  },
  "sentry-read-issues": {
    "$comment": "View issues and events in Sentry.",
    "properties": {
      "method": {
        "const": "GET"
      },
      "path": {
        "type": "string",
        "pattern": "^/api/0/(issues|organizations/[^/]+/issues|projects/[^/]+/[^/]+/(issues|events))(/.*)?$"
      }
    },
    "required": [
      "method",
      "path"
    ]
  },
  "sentry-write-issues": {
    "$comment": "Create, update, or delete Sentry issues.",
    "properties": {
      "method": {
        "enum": [
          "POST",
          "PUT",
          "PATCH",
          "DELETE"
        ]
      },
      "path": {
        "type": "string",
        "pattern": "^/api/0/(issues|organizations/[^/]+/issues|projects/[^/]+/[^/]+/issues)(/.*)?$"
      }
    },
    "required": [
      "method",
      "path"
    ]
  },
  "sentry-read-projects": {
    "$comment": "View Sentry projects.",
    "properties": {
      "method": {
        "const": "GET"
      },
      "path": {
        "type": "string",
        "pattern": "^/api/0/projects(/.*)?$"
      }
    },
    "required": [
      "method",
      "path"
    ]
  },
  "sentry-write-projects": {
    "$comment": "Create, update, or delete Sentry projects.",
    "properties": {
      "method": {
        "enum": [
          "POST",
          "PUT",
          "PATCH",
          "DELETE"
        ]
      },
      "path": {
        "type": "string",
        "pattern": "^/api/0/(projects|teams/[^/]+/[^/]+/projects)(/.*)?$"
      }
    },
    "required": [
      "method",
      "path"
    ]
  },
  "sentry-read-organizations": {
    "$comment": "View Sentry organizations.",
    "properties": {
      "method": {
        "const": "GET"
      },
      "path": {
        "type": "string",
        "pattern": "^/api/0/organizations(/[^/]+)?/?$"
      }
    },
    "required": [
      "method",
      "path"
    ]
  },
  "sentry-read-teams": {
    "$comment": "View Sentry teams.",
    "properties": {
      "method": {
        "const": "GET"
      },
      "path": {
        "type": "string",
        "pattern": "^/api/0/(teams|organizations/[^/]+/teams)(/.*)?$"
      }
    },
    "required": [
      "method",
      "path"
    ]
  },
  "sentry-read-releases": {
    "$comment": "View Sentry releases.",
    "properties": {
      "method": {
        "const": "GET"
      },
      "path": {
        "type": "string",
        "pattern": "^/api/0/(organizations/[^/]+/releases|projects/[^/]+/[^/]+/releases)(/.*)?$"
      }
    },
    "required": [
      "method",
      "path"
    ]
  },
  "sentry-write-releases": {
    "$comment": "Create, update, or delete Sentry releases.",
    "properties": {
      "method": {
        "enum": [
          "POST",
          "PUT",
          "PATCH",
          "DELETE"
        ]
      },
      "path": {
        "type": "string",
        "pattern": "^/api/0/(organizations/[^/]+/releases|projects/[^/]+/[^/]+/releases)(/.*)?$"
      }
    },
    "required": [
      "method",
      "path"
    ]
  }
}
